//Realice un programa que devuelva el área del triángulo usando los siguientes pares de base-altura:
//(1,2) (2,4) (3,6) (4,8) (5, 10) (6,12) (7,14)
//Implemente un método llamado calcularAreaTriangulo que reciba dos números por parámetro (uno llamado base y otro altura)
//(base*altura)/2
let base: number = 1;
let altura: number = 2;
let area: number;
area = (base * altura) / 2;

function dibujar_triangulo(base: number = 0, altura: number = 2): number {
  return area;
}
for (base = 0; index < array.length; index++) {}
console.log(dibujar_triangulo(area));
